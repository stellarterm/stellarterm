import _ from 'lodash';

import Event from '../Event';

export default function CHANGEMEPLEASECHANGEMEPLEASECHANGEMEPLEASECHANGEMEPLEASE(driver) {
  this.event = new Event();

  const init = () => {
    this.state = 'blah';
  };
  init();

  this.handlers = {
  };
}

